name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      SENTRY_DSN: ${{ vars.SENTRY_DSN }}
      SHELLUI_BACKEND_URL: ${{ vars.SHELLUI_BACKEND_URL || 'https://id.shellui.com' }}
      SHELLUI_ADMIN_URL: ${{ vars.SHELLUI_ADMIN_URL || 'https://admin.shellui.com' }}
      SHELLUI_COMPANY_ID: ${{ vars.SHELLUI_COMPANY_ID || '2' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies (root)
        run: pnpm install --frozen-lockfile

      - name: Install dependencies (app)
        run: pnpm install --frozen-lockfile --dir app

      - name: Build
        run: pnpm build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist/web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
