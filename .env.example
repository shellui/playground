# Optional: override the app iframe URL (dev and build).
# SHELLUI_APP_URL=http://localhost:5173

# Optional: dev only – port for the React app (default 5173). Used when SHELLUI_APP_URL is not set.
# SHELLUI_APP_PORT=5173
