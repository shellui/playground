# Optional overrides for `shellui.config.ts` backend (defaults match production demo).
# Copy to `.env` — the ShellUI CLI loads dotenv when you run `pnpm start` / `pnpm build`.
# SHELLUI_BACKEND_URL=https://id.shellui.com
# SHELLUI_ADMIN_URL=https://admin.shellui.com
# SHELLUI_COMPANY_ID=1
